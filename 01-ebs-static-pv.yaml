apiVersion: v1
kind: PersistentVolume
metadata:
  name: ebs-static
spec:
  accessModes:
  - ReadWriteOnce  # at a time we can attach the volume to one ec2 instance only
  capacity:
    storage: 30Gi
  csi: 
    driver: ebs.csi.aws.com
    fsType: ext4
    volumehandle: